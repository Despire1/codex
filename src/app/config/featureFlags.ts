/**
 * Локальные фича-флаги для разработки.
 * Переключаются вручную в коде. Не выносятся в БД, env или localStorage —
 * это «эксперимент рядом с продом», а не пользовательская настройка.
 */
export const SCHEDULE_V2_ENABLED = true;
